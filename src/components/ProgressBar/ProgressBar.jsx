import React from "react";
import ProgressBar from "@commercetools-uikit/progress-bar";
const ProgressBarCt = () => {
  return <ProgressBar label={`${50}% completed`} progress={50} />;
};

export default ProgressBarCt;
