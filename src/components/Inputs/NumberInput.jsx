import React from "react";
import NumberInput from "@commercetools-uikit/number-input";
const NumberInputCt = ({ value, onChange }) => {
  return <NumberInput value={value} onChange={onChange} />;
};

export default NumberInputCt;
