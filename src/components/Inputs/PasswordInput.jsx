import React from "react";
import PasswordInput from "@commercetools-uikit/password-input";

const PasswordInputCt = ({ value, onChange }) => {
  return <PasswordInput value={value} onChange={onChange} />;
};

export default PasswordInputCt;
